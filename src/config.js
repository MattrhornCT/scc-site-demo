// The order form now POSTs to the same-origin Cloudflare Pages Function at
// /order (functions/order.js) instead of an external Apps Script URL.

// Shown as the manual fallback if that submission fails — see OrderForm.jsx.
export const OWNER_EMAIL = 'hello@shelbyscookieco.ca';
