node_modules/
dist/
.DS_Store
*.local

# Airtable build-time fetch: local secrets, cache, and generated output —
# regenerated by `npm run fetch-airtable` before every dev/build.
.env
.cache/
src/data/*.generated.json
public/gallery-generated/

# Wrangler local dev (functions/order.js) — secrets and local KV/state.
.dev.vars
.wrangler/
